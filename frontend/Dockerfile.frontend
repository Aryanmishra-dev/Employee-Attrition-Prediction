FROM nginx:alpine

# The frontend just serves static files from public/ if used separately.
# In the combined deployment, FastAPI serves them.
COPY frontend/public /usr/share/nginx/html/public

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
